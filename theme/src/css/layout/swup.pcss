#swup {
  @apply bg-white min-h-screen relative;
  flex: 1 0 auto;
}

/* purgecss start ignore */
.swup-transition-fade {
  @apply transition-opacity duration-short ease-in-out;
  opacity: 1;
}

html.is-animating .swup-transition-fade {
  opacity: 0;
}
/* purgecss end ignore */